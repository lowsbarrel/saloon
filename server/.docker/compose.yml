services:
  saloon:
    build:
      context: ..
      dockerfile: .docker/Dockerfile
    env_file: .env
    ports:
      - "${SALOON_PORT:-8000}:8000"
    restart: unless-stopped
    read_only: true
    security_opt:
      - no-new-privileges:true
